import React from "react";
import { PhotoCommentStyled } from "../../../styles/Photo";

const PhotoCommentes = ({ id, comments }) => {
  return <PhotoCommentStyled>Comentários</PhotoCommentStyled>;
};

export default PhotoCommentes;
